{
  "name": "model-provider-usage-limits-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace=packages/core && npm run build --workspace=packages/opencode-usage-limits-router",
    "clean": "npm run clean --workspaces",
    "watch": "npm run watch --workspace=packages/core"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
